# Global Skills (Tier 1 - IMMUTABLE)

This file is NEVER modified during a phil-connors loop.
Add project-wide patterns, critical files, and essential rules here.

These skills are injected into EVERY iteration via systemMessage,
ensuring you never lose critical context.

## Project Patterns
<!-- Add your project's coding patterns here -->
- Use camelCase for variables & functions, PascalCase for classes, SCREAMING_CASE for constants, kebab-case for typescript filenames
- all lowercase for landing page copy
- Normal casing for dashboards
- use shadcn components when possible, using CLI to generate and toolcalls to iterate 

## Critical Files
- ./CLAUDE.md

## Essential Rules
<!-- Add rules that must always be followed -->
- Test everything: if it is visible on a page or used in the build, it needs a test
- If you run a command that watches the terminal, make sure it's either a background process or has an appropriate timeout

## Important Context
<!-- Add any context that should persist across all iterations -->
